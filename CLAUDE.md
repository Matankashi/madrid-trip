# מדריד — נובמבר 2026

תיק תכנון לטיול סולו של 12 יום במדריד, בנוי כאתר סטטי מקומי.

## מה זה

ארבעה עמודי HTML שחולקים גיליון סגנון אחד. אין שלב בנייה, אין תלויות,
אין package.json — ה-JS היחיד שנטען חיצונית הוא Firebase דרך CDN.
מוגן בהתחברות: פותחים `index.html`, מקבלים הפניה ל-`login.html` אם
לא מחוברים.

## מבנה

```
index.html          עמוד בית — קישורים, ספירה לאחור למשחק, כפתור התנתקות
dossier.html         מסמך התיק, עשרה פרקים ממוספרים
budget.html          מחשבון תקציב אינטראקטיבי
login.html           מסך התחברות (אימייל+סיסמה), לא מוגן
firestore.rules      כללי האבטחה של Firestore — עותק תיעודי, ראו "Firebase" למטה
assets/
  styles.css         כל הסגנונות של ארבעת העמודים
  budget.js          לוגיקת המחשבון (לא נוגע ב-Firebase)
  dossier.js         רשימת המשימות — Firestore + גיבוי ב-localStorage
  firebase-init.js    אתחול Firebase (app/auth/db), הקובץ היחיד עם ה-config
  auth-guard.js       מפנה ל-login.html אם אין משתמש מחובר
```

## Firebase

הפרויקט משתמש ב-Firebase רק בתור מסד נתונים ואימות — **לא** ב-Firebase
Hosting. האתר עצמו יושב על GitHub Pages.

- **Auth:** אימייל+סיסמה בלבד, בלי הרשמה עצמית. משתמש יחיד, נוצר ידנית
  בקונסולת Firebase.
- **Firestore:** מסמך יחיד למשתמש, ב-`/users/{uid}/state/checklist` —
  שם נשמרים הסימונים ברשימת המשימות של `dossier.html`. מפתחות
  המסמך הם `item0`, `item1`... לפי סדר ה-`data-c` ב-HTML, בדיוק כמו
  שהיה קודם ב-localStorage — **שינוי סדר או הוספת/מחיקת פריט ב-`data-c`
  מזיז את הסימונים של הפריטים שאחריו.**
- **כללי אבטחה:** `firestore.rules` הוא **עותק תיעודי בלבד**. אין
  `firebase.json` ואין פריסה דרך ה-CLI — הכללים מתעדכנים ידנית דרך
  Firestore → Rules בקונסולה, ואז מעתיקים את התוכן העדכני חזרה לקובץ
  הזה כדי שישאר מסונכרן. אל תניחו שהקובץ בהכרח תואם למה שפרוס.
- **גרסת ה-SDK מוצמדת:** כל הייבוא הוא מ-
  `https://www.gstatic.com/firebasejs/12.18.0/firebase-*.js` (ה-CDN
  המודולרי הרשמי, בלי npm ובלי בנדלר). לעדכון גרסה — לשנות את המספר
  בכל מקום שהוא מופיע: `assets/firebase-init.js`, `assets/auth-guard.js`,
  `assets/dossier.js`, `login.html`, `index.html`.
- **דפים מוגנים:** `index.html`, `dossier.html`, `budget.html` טוענים
  את `assets/auth-guard.js` כ-module script ומתחילים עם
  `style="visibility:hidden"` על ה-`<body>` כדי למנוע הבזק של תוכן
  לפני שהאימות נפתר. `login.html` הוא הדף היחיד שלא מוגן.

## עובדות הטיול

הנתונים האלה מופיעים בכמה מקומות. אם משנים אותם, לעדכן בכל המקומות.

| פרט | ערך |
|---|---|
| תאריכים | 2–13 בנובמבר 2026, 12 ימים / 11 לילות |
| המשחק המרכזי | אתלטיקו מדריד–ברצלונה, 8.11.2026, 20:00 שעון מדריד |
| אצטדיון | Riyadh Air Metropolitano, קו 7 של המטרו |
| חג עירוני | אלמודנה, 9.11, יום שני |
| ליגת אלופות | מחזור 4 ב-3–4.11 (תלוי הגרלה), מחזור 5 ב-24–25.11 |
| הרכב | סולו |
| תקציב | €1,720 חסכוני · €2,870 מאוזן · €4,965 נוח |
| שכונות מועמדות | מלסניה, צ'מברי, לה לטינה |

## מוסכמות

- **עברית ו-RTL.** ה-`<html>` תמיד עם `lang="he" dir="rtl"`. טקסט חדש בעברית.
- **מספרים ומחירים תמיד LTR.** משתמשים במחלקה `.mono` או `.num` בטבלאות,
  שמגדירות `direction:ltr`. בלי זה סימני מטבע ומקפים קופצים לצד הלא נכון.
- **צבעים רק דרך משתני CSS.** כל הפלטה מוגדרת ב-`:root` בראש `styles.css`.
  אין ערכי hex מקודדים בגוף הקוד — הצבעים הבודדים שכן מקודדים נמצאים
  במערך `GROUPS` ב-`budget.js` ובתגי `.dot`, וצריכים להישאר מסונכרנים איתו.
- **סגנונות ספציפיים לעמוד** מסומנים בקלאס על ה-`<body>`:
  `p-home`, `p-doc`, `p-budget`, `p-login`. `h2` מעוצב אחרת בכל אחד.
- **JavaScript וניל בלבד.** בלי React, בלי בנדלר, בלי npm.
- **גופנים** נטענים מ-Google Fonts: Suez One לכותרות, Assistant לגוף,
  IBM Plex Mono למספרים. בלי אינטרנט העמוד עדיין עובד, רק נראה אחרת.

## הרצה

**פתיחה ישירה מ-`file://` לא עובדת יותר** — ה-Firebase SDK (auth
במיוחד) דורש origin http/https אמיתי. להרצה מקומית:

```bash
python3 -m http.server 8000
# ואז http://localhost:8000
```

הכניסה הראשונה תפנה ל-`login.html`. המשתמש נוצר ידנית בקונסולת
Firebase — אין הרשמה עצמית באתר.

## דברים שלא לעשות

- לא להוסיף שלב בנייה או מנהל חבילות. הערך של הפרויקט הוא שהוא קבצים
  סטטיים בלי כלום מעבר ל-Firebase דרך CDN.
- לא לפצל את `styles.css` לקבצים לפי עמוד. נקודת אמת אחת לעיצוב.
- לא להסיר את ה-`try/catch` סביב `localStorage` ב-`dossier.js` — זה
  הגיבוי האופליין; בלעדיו כשל אחסון (למשל דפדפן חוסם) יפיל את העמוד.
- לא להוסיף `firebase.json` בחזרה או לחבר `firebase deploy` — האחסון
  הוא GitHub Pages בלבד, Firebase הוא מסד נתונים ואימות ותו לא.
- לא להוסיף הרשמה עצמית ל-`login.html`. משתמש יחיד, נוצר ידנית.

## רעיונות להמשך

- טבלת מעקב מחירי טיסות עם הזנה ידנית לאורך זמן
- מפה של השכונות המועמדות עם המקומות שכבר נאספו
- לוח 12 הימים כתצוגת יומן במקום טבלה
- ייצוא התקציב ל-CSV
