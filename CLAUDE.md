# מדריד — נובמבר 2026

**גרסה: v1.8**

תיק תכנון לטיול סולו של 12 יום במדריד, בנוי כאתר סטטי מקומי.

## מה זה

ארבעה עמודי HTML שחולקים גיליון סגנון אחד. אין שלב בנייה, אין תלויות,
אין package.json — ה-JS היחיד שנטען חיצונית הוא Firebase דרך CDN.
מוגן בהתחברות: פותחים `index.html`, מקבלים הפניה ל-`login.html` אם
לא מחוברים.

## מבנה

```
index.html          עמוד בית — קישורים, ספירה לאחור למשחק, כפתור התנתקות
dossier.html         מסמך התיק, עשרה פרקים ממוספרים
budget.html          מחשבון תקציב אינטראקטיבי
login.html           מסך התחברות (אימייל+סיסמה), לא מוגן
firestore.rules      כללי האבטחה של Firestore — עותק תיעודי, ראו "Firebase" למטה
assets/
  styles.css         כל הסגנונות של ארבעת העמודים
  budget.js          לוגיקת המחשבון (לא נוגע ב-Firebase)
  dossier.js         רשימת המשימות — Firestore + גיבוי ב-localStorage
  firebase-init.js    אתחול Firebase (app/auth/db), הקובץ היחיד עם ה-config
  auth-guard.js       מפנה ל-login.html אם אין משתמש מחובר
```

## Firebase

הפרויקט משתמש ב-Firebase רק בתור מסד נתונים ואימות — **לא** ב-Firebase
Hosting. האתר עצמו יושב על GitHub Pages.

- **Auth:** אימייל+סיסמה בלבד, בלי הרשמה עצמית. משתמש יחיד, נוצר ידנית
  בקונסולת Firebase.
- **Firestore:** שני מסמכים למשתמש, שניהם תחת `/users/{uid}/state/`:
  - `checklist` — הסימונים ברשימת המשימות של `dossier.html`. מפתחות
    המסמך הם `item0`, `item1`... לפי סדר ה-`data-c` ב-HTML —
    **שינוי סדר או הוספת/מחיקת פריט ב-`data-c` מזיז את הסימונים של
    הפריטים שאחריו.**
  - `budget` — כל הערכים, המטבעות, השערים, הנעילות והמתגים של
    `budget.html`, ב-`assets/budget.js`. מבנה
    `{tier:'lean'|'mid'|'rich'|'custom', custom:{values:{...}, currencies:{...}}, toggles:{...}, rates:{usd:{value,updatedAt}, ils:{value,updatedAt}}, locks:{id:{amount,currency,rate,chargedOn}}}`.
    **שכבות תקציב:** `PRESETS` (חסכוני/מאוזן/נוח) הם קבועי קוד בלבד —
    לעולם לא נכתבים ל-Firestore, רק `custom` (המספרים של המשתמש) ו-`tier`
    (איזו שכבה מוצגת כרגע) נשמרים. לחיצה על שכבת מחיר כותבת את מחיר
    הייחוס שלה (מומר למטבע שכבר נבחר לשורה, ראו `presetAmountForRow`)
    לתוך כל שורה — **כולל שורה נעולה**: נעילה קיימת רק ב-`tier==='custom'`
    (`isLockActive(id)`), אז בשכבת מחיר קבועה שורה נעולה מוצגת בדיוק
    כמו שורה פתוחה, עם מחיר הייחוס של השכבה, לא הסכום ששולם בפועל —
    ראו "נעילה" למטה. עריכת כל שדה
    (כולל ימים/לילות/מספר טיולים/בורר מטבע) בזמן ששכבת מחיר פעילה
    מעבירה אוטומטית ל-`custom`, עם הערכים המוצגים כרגע כנקודת פתיחה.
    מעבר **מ**-custom **ל**שכבת מחיר דורש אישור (`confirm()` בעברית) —
    זו הפעולה היחידה שמוחקת בפועל את `custom` (לשורות הפתוחות); מעבר
    בין שתי שכבות מחיר, או חזרה ל-custom, לא. מסמך ישן (v1.2–v1.4, בלי
    `tier`/`custom`) נטען עם `tier:'custom'` ו-`custom.values` = מה שהיה
    ב-`values` הישן — אין דרך לדעת אם זה תואם במקרה לשכבה, אז ברירת
    המחדל הבטוחה שלא מסתכנת במחיקה היא custom. תחת הסה"כ מופיע קו
    השוואה (`#tierCompare`, מוצג רק כש-`tier==='custom'`) שמשווה את
    הסכום האמיתי מול "כמה זה היה עולה" בכל שכבה — עם אותם `toggles`
    (שורה שכובתה לא נכנסת גם להשוואה) אבל `presetTotalEur` טהור מ-PRESETS
    בלבד: שורה נעולה תורמת שם את מחיר הייחוס של השכבה, לא הסכום הקפוא
    האמיתי — התכנון המקורי לא משתנה בגלל מה שקרה בפועל מאז (תוקן ב-`v1.7`,
    ראו "נעילה" למטה — לפני זה שורה נעולה דלפה עם הסכום האמיתי שלה
    לתוך שלוש ההשוואות, מה שהפך אותן ללא-שימושיות).
    **ייחוס לכל שורה** (`.tier-ref`, מוצג רק כש-`tier==='custom'` וגם
    `showTierRefs` דלוק — שני התנאים, לא רק אחד): מציג את שלושת מחירי
    השכבות לצד הערך האמיתי של השורה. תמיד ביורו (זה המטבע של ה-PRESETS
    — לא ממירים למטבע השורה, זו נקודת ייחוס קבועה ולא חישוב), ותמיד
    ליחידה (מחיר ללילה/ליום/לטיול כמו שהשדה עצמו מציג, לא מוכפל
    ב-nights/days/tripCount). עובד גם על שורה נעולה — דווקא שם הכי
    שימושי (משווים בין מה ששולם בפועל למה שתוכנן). `showTierRefs`
    (ברירת מחדל: מוצג) הוא מתג יחיד שנשמר ל-Firestore ומשפיע על כל
    השורות יחד — קריאה בלבד, לעולם לא נכנס לשום חישוב.
    כל שורת עלות (חוץ מ-`tripCount`, שהיא רק מספר נסיעות) יש לה בורר
    מטבע — EUR כברירת מחדל, או USD/ILS. שני שדות שער ידניים, בכיוון
    €→X (1 יורו = X דולר/שקל): `rates.usd` (טיסות וכו') ו-`rates.ils`,
    כל אחד עם `updatedAt` (תאריך ISO) שמתעדכן אוטומטית כשמשנים את הערך
    — לא שדה שממלאים ידנית — או כשלוחצים "↻ עדכן" ליד השדה, שמושך שער
    חי מ-`https://open.er-api.com/v6/latest/EUR` (ללא מפתח, ללא הרצה
    אוטומטית — רק בלחיצה). כישלון בעדכון משאיר את הערך והתאריך כמו
    שהיו ומציג שגיאה קטנה באדום ליד השדה — אף פעם לא מרוקן את השדה.
    שורה בלי `currencies[id]` תקין (מסמך ישן מלפני הפיצ׳ר, או שדה חדש
    שנוסף אחרי השמירה האחרונה) נופלת ל-EUR; שער חסר/אפס/לא-מספרי נופל
    לברירת מחדל בטוחה (1.08 / 4.05) כדי שלא יזלוג NaN/Infinity לסכומים.
    מסמך ישן מאוד (`values.rate` בודד, מלפני שער ה-USD) עובר מיגרציה
    חד-פעמית ל-`rates.ils.value` עם `updatedAt: null` (מוצג כ"טרם עודכן").
    **נעילה** (`locks[id]`): שורה שכבר שולמה בפועל — כפתור מנעול לצד
    השורה שומר תמונת מצב קפואה: `amount` הוא הסכום **המלא** של השורה
    (אחרי כפל לילות/ימים/מספר טיולים אם רלוונטי — לא מחיר ליחידה),
    `currency` ו-`rate` הם המטבע והשער בו הומר באותו רגע (`rate:1` אם
    כבר EUR), ו-`chargedOn` תאריך חיוב שהמשתמש עורך ידנית (בכוונה לא
    בתוך `values` — הוא חלק מרשומת הנעילה הקפואה, לא שדה טופס רגיל,
    אחרת הוא לא נשמר ולא מגיע למכשירים אחרים).
    **נעילה קיימת ומשמעותית רק ב-`custom`** (`isLockActive(id)` =
    `!!locks[id] && tier==='custom'`, `v1.8`) — בשכבת מחיר קבועה היא
    כאילו לא קיימת בכלל: לא בערך המוצג בשורה, לא בסכום שנכנס לקבוצה/
    לסה"כ/ל"שולם", ולא בכפתור המנעול עצמו, שמוסתר לגמרי מחוץ ל-`custom`
    (`lockBtn.hidden = tier !== 'custom'` ב-`render()`) — נעילה חסרת
    משמעות בשכבת מחיר, ולכן אין דרך לפתוח/לנעול שורה משם. כל עוד שורה
    נעולה **וב-custom**, `lineValueNative`/`lineValueEur` קוראות מ-
    `locks[id]` בלבד ומתעלמות לגמרי משדה הכמות, בורר המטבע, ומשני שדות
    השער (כולל כפתור "↻ עדכן") — שינוי שער אחרי נעילה לא זז אף אגורה
    מהסכום הנעול, ומעבר לשכבת מחיר ובחזרה לא נוגע ב-`locks[id]` או
    ב-`customValues[id]` בכלל (`getState` קוראת מה-DOM ל-`customValues`
    רק כש-`tier==='custom'` — בשכבת מחיר ה-DOM מציג את מחיר הייחוס,
    לא את הסכום האמיתי, וכתיבתו היה דורס אותו). מסמך בלי `locks` בכלל
    (מלפני הפיצ׳ר) → כל השורות פתוחות; שורה נעולה בלי `chargedOn` (לא
    אמור לקרות) מוצגת עם שדה תאריך ריק, לא קורסת.
    הטעינה היא `getDoc` חד-פעמי
    (לא `onSnapshot` מתמשך כמו הצ'קליסט) כדי שהד של כתיבה קודמת לא
    ידרוס עריכה שהמשתמש עושה בו-זמנית — לכן אין סנכרון חי בין מכשירים
    פתוחים בו-זמנית לעמוד הזה, רק רענון בטעינת דף. שדה שחסר במסמך
    (כי נוסף input חדש אחרי שהמסמך נכתב) נופל חזרה לברירת המחדל שב-HTML.
  - שני המסמכים משתמשים באותו דפוס: כתיבה מבוזרת (debounce 800ms)
    וגיבוי אופליין ב-localStorage.
- **כללי אבטחה:** `firestore.rules` מתיר כתיבה וקריאה בכל
  `/users/{uid}/**` (לא רק מסמך בודד) — הורחב כשנוסף `state/budget`
  כדי לא לדרוש שינוי כללים בכל מסמך חדש. הקובץ הוא **עותק תיעודי
  בלבד**. אין `firebase.json` ואין פריסה דרך ה-CLI — הכללים
  מתעדכנים ידנית דרך Firestore → Rules בקונסולה, ואז מעתיקים את
  התוכן העדכני חזרה לקובץ הזה כדי שישאר מסונכרן. אל תניחו שהקובץ
  בהכרח תואם למה שפרוס.
- **גרסת ה-SDK מוצמדת:** כל הייבוא הוא מ-
  `https://www.gstatic.com/firebasejs/12.18.0/firebase-*.js` (ה-CDN
  המודולרי הרשמי, בלי npm ובלי בנדלר). לעדכון גרסה — לשנות את המספר
  בכל מקום שהוא מופיע: `assets/firebase-init.js`, `assets/auth-guard.js`,
  `assets/dossier.js`, `login.html`, `index.html`.
- **דפים מוגנים:** `index.html`, `dossier.html`, `budget.html` טוענים
  את `assets/auth-guard.js` כ-module script ומתחילים עם
  `style="visibility:hidden"` על ה-`<body>` כדי למנוע הבזק של תוכן
  לפני שהאימות נפתר. `login.html` הוא הדף היחיד שלא מוגן.

## עובדות הטיול

הנתונים האלה מופיעים בכמה מקומות. אם משנים אותם, לעדכן בכל המקומות.

| פרט | ערך |
|---|---|
| תאריכים | 2–13 בנובמבר 2026, 12 ימים / 11 לילות |
| המשחק המרכזי | אתלטיקו מדריד–ברצלונה, 8.11.2026, 20:00 שעון מדריד |
| אצטדיון | Riyadh Air Metropolitano, קו 7 של המטרו |
| חג עירוני | אלמודנה, 9.11, יום שני |
| ליגת אלופות | מחזור 4 ב-3–4.11 (תלוי הגרלה), מחזור 5 ב-24–25.11 |
| הרכב | סולו |
| תקציב | €1,720 חסכוני · €2,870 מאוזן · €4,965 נוח |
| שכונות מועמדות | מלסניה, צ'מברי, לה לטינה |

## מוסכמות

- **עברית ו-RTL.** ה-`<html>` תמיד עם `lang="he" dir="rtl"`. טקסט חדש בעברית.
- **מספרים ומחירים תמיד LTR.** משתמשים במחלקה `.mono` או `.num` בטבלאות,
  שמגדירות `direction:ltr`. בלי זה סימני מטבע ומקפים קופצים לצד הלא נכון.
- **צבעים רק דרך משתני CSS.** כל הפלטה מוגדרת ב-`:root` בראש `styles.css`.
  אין ערכי hex מקודדים בגוף הקוד — הצבעים הבודדים שכן מקודדים נמצאים
  במערך `GROUPS` ב-`budget.js` ובתגי `.dot`, וצריכים להישאר מסונכרנים איתו.
- **סגנונות ספציפיים לעמוד** מסומנים בקלאס על ה-`<body>`:
  `p-home`, `p-doc`, `p-budget`, `p-login`. `h2` מעוצב אחרת בכל אחד.
- **JavaScript וניל בלבד.** בלי React, בלי בנדלר, בלי npm.
- **גופנים** נטענים מ-Google Fonts: Suez One לכותרות, Assistant לגוף,
  IBM Plex Mono למספרים. בלי אינטרנט העמוד עדיין עובד, רק נראה אחרת.

## הרצה

**פתיחה ישירה מ-`file://` לא עובדת יותר** — ה-Firebase SDK (auth
במיוחד) דורש origin http/https אמיתי. להרצה מקומית:

```bash
python3 -m http.server 8000
# ואז http://localhost:8000
```

הכניסה הראשונה תפנה ל-`login.html`. המשתמש נוצר ידנית בקונסולת
Firebase — אין הרשמה עצמית באתר.

## דברים שלא לעשות

- לא להוסיף שלב בנייה או מנהל חבילות. הערך של הפרויקט הוא שהוא קבצים
  סטטיים בלי כלום מעבר ל-Firebase דרך CDN.
- לא לפצל את `styles.css` לקבצים לפי עמוד. נקודת אמת אחת לעיצוב.
- לא להסיר את ה-`try/catch` סביב `localStorage` ב-`dossier.js` — זה
  הגיבוי האופליין; בלעדיו כשל אחסון (למשל דפדפן חוסם) יפיל את העמוד.
- לא להוסיף `firebase.json` בחזרה או לחבר `firebase deploy` — האחסון
  הוא GitHub Pages בלבד, Firebase הוא מסד נתונים ואימות ותו לא.
- לא להוסיף הרשמה עצמית ל-`login.html`. משתמש יחיד, נוצר ידנית.

## רעיונות להמשך

- טבלת מעקב מחירי טיסות עם הזנה ידנית לאורך זמן
- מפה של השכונות המועמדות עם המקומות שכבר נאספו
- לוח 12 הימים כתצוגת יומן במקום טבלה
- ייצוא התקציב ל-CSV
